{
  "name": "space365-module",
  "version": "0.1.0",
  "private": true,
  "description": "Space365 SpacetimeDB module — authoritative world state",
  "scripts": {
    "build": "spacetime build",
    "publish:local": "spacetime publish space365 --server local --yes"
  },
  "dependencies": {
    "spacetimedb": "2.6.*"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
