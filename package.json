{
  "name": "space365",
  "private": true,
  "version": "0.0.1",
  "description": "Space365 monorepo scaffold",
  "scripts": {
    "test": "bun test",
    "test:parity": "bun run parity/publish.ts && bun test parity",
    "graph:validate-credentials": "bun run scripts/validate-graph-credentials.ts"
  },
  "devDependencies": {
    "spacetimedb": "2.6.1"
  }
}
